import type { FxAlert } from "./fx-alert-types"

export function notifyFxAlertTriggered(_alert: FxAlert, _currentRate: number): void {
  // Stub: implement notification (e.g. toast, push) as needed
}
